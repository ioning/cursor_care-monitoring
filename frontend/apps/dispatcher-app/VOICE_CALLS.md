# Голосовые вызовы в приложении диспетчера

## Обзор

В приложении диспетчера реализована поддержка голосовых вызовов через WebRTC с возможностью использования внешних устройств (Bluetooth гарнитуры, наушники, USB микрофоны).

## Функциональность

### Основные возможности

1. **Инициализация голосового вызова**
   - Автоматическое подключение к микрофону
   - Создание WebRTC соединения
   - Обмен SDP offer/answer

2. **Управление внешними устройствами**
   - Выбор микрофона (встроенный, USB, Bluetooth)
   - Выбор динамиков/наушников
   - Автоматическое определение доступных устройств
   - Переключение устройств во время звонка

3. **Управление звонком**
   - Включение/выключение микрофона
   - Завершение звонка
   - Индикация состояния

## Архитектура

### Компоненты

#### 1. VoiceCallService (`src/services/voiceCall.service.ts`)

Основной сервис для управления голосовыми вызовами:

```typescript
// Инициализация вызова
await voiceCallService.initializeCall({
  callId: '...',
  wardId: '...',
  dispatcherId: '...',
  wardPhone: '...'
});

// Переключение устройства ввода
await voiceCallService.setInputDevice(deviceId);

// Переключение устройства вывода
await voiceCallService.setOutputDevice(deviceId);

// Управление микрофоном
voiceCallService.toggleMute();

// Завершение вызова
await voiceCallService.endCall();
```

#### 2. VoiceCall Component (`src/components/VoiceCall.vue`)

UI компонент для управления звонком:
- Кнопки управления (микрофон, завершить)
- Выбор устройств ввода/вывода
- Индикация состояния

#### 3. API методы (`src/api/dispatcher.api.ts`)

```typescript
// Инициализация вызова
dispatcherApi.initiateVoiceCall(callId)

// Отправка WebRTC offer
dispatcherApi.sendVoiceOffer(callId, offer)

// Отправка WebRTC answer
dispatcherApi.sendVoiceAnswer(callId, answer)

// Отправка ICE кандидата
dispatcherApi.sendIceCandidate(callId, candidate)

// Завершение вызова
dispatcherApi.endVoiceCall(callId)
```

## Использование

### В CallDetailView

Голосовой вызов автоматически доступен для вызовов со статусом `in_progress`:

```vue
<template>
  <!-- Кнопка для начала звонка -->
  <button
    v-if="selectedCall.status === 'in_progress' && !isVoiceCallActive"
    @click="startVoiceCall"
    class="btn btn-primary"
  >
    📞 Начать звонок
  </button>

  <!-- Компонент голосового вызова -->
  <VoiceCall
    v-if="isVoiceCallActive && selectedCall"
    :call-id="selectedCall.id"
    :ward-id="selectedCall.wardId"
    :dispatcher-id="selectedCall.dispatcherId || authStore.user?.id || ''"
    :ward-phone="selectedCall.ward?.emergencyContact"
    @call-ended="handleVoiceCallEnded"
  />
</template>
```

## Поддержка внешних устройств

### Bluetooth гарнитуры

Система автоматически определяет подключенные Bluetooth устройства:

1. **Автоматическое определение**
   - При подключении Bluetooth гарнитуры она появляется в списке устройств
   - Можно выбрать в качестве устройства ввода или вывода

2. **Переключение во время звонка**
   - Можно переключиться на Bluetooth гарнитуру в любой момент
   - Изменения применяются немедленно

### USB микрофоны и наушники

Аналогично Bluetooth устройствам:
- Автоматическое определение при подключении
- Возможность выбора в списке устройств
- Переключение во время звонка

## WebRTC конфигурация

### STUN серверы

По умолчанию используются публичные Google STUN серверы:
- `stun:stun.l.google.com:19302`
- `stun:stun1.l.google.com:19302`

### TURN серверы (для production)

Для работы через NAT и файрволы рекомендуется добавить TURN серверы:

```typescript
{
  urls: 'turn:your-turn-server.com:3478',
  username: 'user',
  credential: 'pass'
}
```

## Безопасность

1. **Разрешения браузера**
   - Требуется разрешение на доступ к микрофону
   - Разрешение запрашивается автоматически при инициализации вызова

2. **HTTPS**
   - WebRTC требует HTTPS для работы (кроме localhost)
   - В production необходимо использовать SSL сертификат

## Обработка ошибок

Сервис обрабатывает следующие ошибки:

1. **Отсутствие доступа к микрофону**
   - Показывается сообщение об ошибке
   - Вызов не инициализируется

2. **Ошибки WebRTC**
   - Автоматическое переподключение при разрыве соединения
   - Логирование ошибок для отладки

3. **Ошибки API**
   - Продолжение работы даже при ошибках отправки данных
   - Логирование для последующего анализа

## Производительность

### Оптимизации

1. **Эхо-подавление**
   - Включено по умолчанию
   - Улучшает качество звука

2. **Подавление шума**
   - Автоматическая фильтрация фонового шума
   - Улучшает разборчивость речи

3. **Автоматическая регулировка усиления**
   - Нормализация уровня звука
   - Обеспечивает комфортный уровень громкости

## Будущие улучшения

1. **Видео звонки**
   - Добавление поддержки видео через WebRTC
   - Возможность видеть подопечного

2. **Запись звонков**
   - Сохранение аудио для последующего анализа
   - Соблюдение требований конфиденциальности

3. **Групповые звонки**
   - Поддержка конференц-звонков
   - Несколько диспетчеров одновременно

4. **Интеграция с телефонными системами**
   - Подключение к традиционным телефонным линиям
   - Интеграция с VoIP провайдерами

## Отладка

### Логирование

Все важные события логируются в консоль:
- Инициализация вызова
- Изменение состояния соединения
- Переключение устройств
- Ошибки

### Проверка устройств

Для проверки доступных устройств:

```javascript
const devices = await voiceCallService.getAudioDevices();
console.log('Input devices:', devices.inputs);
console.log('Output devices:', devices.outputs);
```

## Требования

- Современный браузер с поддержкой WebRTC
- Доступ к микрофону
- HTTPS (для production)
- STUN/TURN серверы для работы через NAT

## Поддержка браузеров

- ✅ Chrome/Edge (последние версии)
- ✅ Firefox (последние версии)
- ✅ Safari (iOS 11+, macOS)
- ⚠️ Internet Explorer (не поддерживается)

---

**Последнее обновление:** 2025-12-25

