// Global test setup
beforeAll(async () => {
  // Setup test environment if needed
});

afterAll(async () => {
  // Cleanup test environment if needed
});

beforeEach(() => {
  // Reset mocks before each test
  jest.clearAllMocks();
});

