# Инструкция по установке Care Monitoring System (Windows)

Пошаговая инструкция по установке всех зависимостей и настройке окружения на Windows.

## 📋 Содержание

1. [Системные требования](#системные-требования)
2. [Установка Node.js и npm](#установка-nodejs-и-npm)
3. [Установка Docker](#установка-docker)
4. [Установка Git](#установка-git)
5. [Установка зависимостей проекта](#установка-зависимостей-проекта)
6. [Настройка окружения](#настройка-окружения)
7. [Проверка установки](#проверка-установки)
8. [Troubleshooting](#troubleshooting)

---

## Системные требования

### Минимальные требования

- **ОС**: Windows 10 (версия 1903+) или Windows 11
- **CPU**: 4 ядра (64-bit процессор с поддержкой виртуализации)
- **RAM**: 8 GB
- **Disk**: 20 GB свободного места
- **Интернет**: для загрузки зависимостей

### Рекомендуемые требования

- **ОС**: Windows 11
- **CPU**: 8+ ядер (64-bit процессор с поддержкой виртуализации)
- **RAM**: 16+ GB
- **Disk**: 100+ GB SSD
- **Интернет**: стабильное соединение
- **WSL2**: рекомендуется для лучшей совместимости с Docker

---

## Установка Node.js и npm

### Вариант 1: Через официальный установщик (рекомендуется)

1. Откройте [nodejs.org](https://nodejs.org/)
2. Скачайте LTS версию (рекомендуется v18.x или выше)
3. Запустите установщик (`.msi` файл)
4. Следуйте инструкциям установщика
5. **Важно**: Убедитесь, что отмечена опция "Add to PATH"
6. Перезапустите PowerShell или командную строку

**Проверка установки:**

```powershell
node --version  # Должно быть v18.x.x или выше
npm --version   # Должно быть 9.x.x или выше
```

### Вариант 2: Через Chocolatey

Если у вас установлен Chocolatey:

```powershell
# Установка Node.js LTS
choco install nodejs-lts

# Проверка
node --version
npm --version
```

### Вариант 3: Через winget (Windows 11)

```powershell
# Поиск доступных версий
winget search nodejs

# Установка Node.js LTS
winget install OpenJS.NodeJS.LTS

# Проверка
node --version
npm --version
```

### Настройка npm (опционально)

Для ускорения установки пакетов можно настроить registry:

```powershell
# Использование зеркала (если основное медленное)
npm config set registry https://registry.npmmirror.com

# Или вернуться к официальному
npm config set registry https://registry.npmjs.org/

# Проверка текущего registry
npm config get registry
```

---

## Установка Docker

### Вариант 1: Docker Desktop для Windows (рекомендуется)

1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)
2. Запустите установщик `Docker Desktop Installer.exe`
3. Следуйте инструкциям установщика
4. **Важно**: Убедитесь, что включена опция "Use WSL 2 instead of Hyper-V" (рекомендуется)
5. Перезапустите компьютер при запросе
6. Запустите Docker Desktop из меню "Пуск"
7. Дождитесь полной загрузки (иконка Docker в системном трее должна быть зелёной)

**Проверка установки:**

```powershell
docker --version
docker compose version
docker run hello-world
```

### Вариант 2: Установка через Chocolatey

```powershell
choco install docker-desktop
```

### Настройка WSL2 (если ещё не установлен)

Docker Desktop требует WSL2. Если он не установлен:

1. Откройте PowerShell от имени администратора
2. Выполните:

```powershell
wsl --install
```

3. Перезапустите компьютер
4. После перезагрузки установится Ubuntu по умолчанию
5. Создайте пользователя Ubuntu при первом запуске

**Проверка WSL2:**

```powershell
wsl --list --verbose
# Должно показать версию 2 для установленных дистрибутивов
```

---

## Установка Git

### Вариант 1: Официальный установщик

1. Скачайте Git с [git-scm.com](https://git-scm.com/download/win)
2. Запустите установщик
3. Следуйте инструкциям (рекомендуются настройки по умолчанию)
4. Выберите "Git from the command line and also from 3rd-party software" в настройках PATH

### Вариант 2: Через Chocolatey

```powershell
choco install git
```

### Вариант 3: Через winget

```powershell
winget install Git.Git
```

**Проверка:**

```powershell
git --version
```

### Настройка Git (первый запуск)

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

---

## Установка зависимостей проекта

### 1. Клонирование репозитория

#### Что такое клонирование?

**Клонирование (git clone)** — это процесс скачивания всех файлов проекта с удалённого сервера (GitHub, GitLab и т.д.) на ваш компьютер вместе со всей историей изменений.

#### Как это работает?

1. **Откуда клонируются файлы:**
   - Файлы скачиваются с удалённого Git-сервера (GitHub, GitLab, Bitbucket и т.д.)
   - Сервер хранит полную копию проекта со всей историей изменений
   - Вы получаете локальную копию всего проекта

2. **Что происходит при клонировании:**
   ```
   Удалённый сервер (GitHub/GitLab) 
         ↓
   [git clone команда] ← Вы отправляете запрос
         ↓
   Скачивание всех файлов
         ↓
   Ваш компьютер
         ↓
   Создаётся папка "care-monitoring" с проектом
   ```

3. **Что появляется в результате:**
   - Создаётся новая папка с именем репозитория (например, `care-monitoring`)
   - В этой папке все файлы проекта
   - Скрытая папка `.git` с историей изменений
   - Все ветки, коммиты, теги проекта

#### Структура до и после клонирования

**До клонирования:**
```
C:\projects\
  (пусто или другие проекты)
```

**Выполнение команды:**
```powershell
cd C:\projects
git clone https://github.com/your-username/care-monitoring.git
```

**После клонирования:**
```
C:\projects\
  └── care-monitoring\          ← Новая папка создана автоматически!
      ├── .git\                 ← История изменений (скрытая папка)
      ├── api-gateway\
      ├── microservices\
      ├── frontend\
      ├── infrastructure\
      ├── shared\
      ├── docs\
      ├── package.json
      ├── README.md
      └── ... (все файлы проекта)
```

#### Подробная инструкция

**Важно:** Выполняйте команду `git clone` в той директории, где вы хотите создать папку проекта. Обычно это:
- Домашняя директория: `C:\Users\ВашеИмя\`
- Рабочая папка: `C:\dev\` или `C:\projects\`
- Документы: `C:\Users\ВашеИмя\Documents\`

**В PowerShell:**

```powershell
# 1. Перейдите в нужную директорию (например, в папку для проектов)
cd C:\projects
# Или создайте папку для проектов:
# mkdir C:\dev
# cd C:\dev

# 2. Проверьте текущую директорию
Get-Location
# Должно показать: C:\projects (или вашу папку)

# 3. Клонирование репозитория через HTTPS
# Git скачает все файлы с сервера и создаст папку "care-monitoring"

# Примеры URL:
# GitHub: git clone https://github.com/your-username/care-monitoring.git
# GitLab: git clone https://gitlab.com/your-username/care-monitoring.git
# Bitbucket: git clone https://bitbucket.org/your-username/care-monitoring.git

git clone https://github.com/your-username/care-monitoring.git

# Что вы увидите в процессе:
# Cloning into 'care-monitoring'...
# remote: Enumerating objects: 1234, done.
# remote: Counting objects: 100% (1234/1234), done.
# remote: Compressing objects: 100% (890/890), done.
# remote: Total 1234 (delta 344), reused 1234 (delta 344), pack-reused 0
# Receiving objects: 100% (1234/1234), 5.67 MiB | 2.45 MiB/s, done.
# Resolving deltas: 100% (344/344), done.

# 4. После клонирования перейдите в директорию проекта
cd care-monitoring

# 5. Проверьте текущую директорию
Get-Location
# Должно показать: C:\projects\care-monitoring

# 6. Посмотрите содержимое папки
Get-ChildItem
# Вы увидите все файлы и папки проекта
```

#### Визуализация процесса

```
ШАГ 1: Вы находитесь в C:\projects
┌─────────────────────┐
│ C:\projects\        │
│   (пусто)           │
└─────────────────────┘

ШАГ 2: Выполняете: git clone https://github.com/.../care-monitoring.git
┌─────────────────────┐
│ C:\projects\        │
│   [скачивание...]   │ ← Git скачивает файлы
└─────────────────────┘
         ↓
ШАГ 3: Процесс завершён
┌─────────────────────┐
│ C:\projects\        │
│   └── care-monitoring\ ← Папка создана автоматически!
│       ├── api-gateway\
│       ├── microservices\
│       ├── frontend\
│       └── ... (все файлы)
└─────────────────────┘
```

#### Откуда именно клонируются файлы?

1. **GitHub:**
   - URL: `https://github.com/username/repository.git`
   - Файлы хранятся на серверах GitHub (Microsoft)
   - Доступ через интернет

2. **GitLab:**
   - URL: `https://gitlab.com/username/repository.git`
   - Файлы хранятся на серверах GitLab
   - Доступ через интернет

3. **Локальный сервер (корпоративный):**
   - URL: `https://git.company.com/project/repository.git`
   - Файлы на внутреннем сервере компании
   - Доступ через внутреннюю сеть или VPN

4. **Локальный репозиторий:**
   - URL: `C:\path\to\repository.git` или `\\server\share\repository.git`
   - Файлы на вашем компьютере или сетевом диске

#### Что содержит склонированный проект?

После успешного клонирования вы получите:
- ✅ Все исходные файлы проекта
- ✅ Все папки и подпапки
- ✅ Файлы конфигурации (package.json, tsconfig.json и т.д.)
- ✅ Документацию (README.md, docs/)
- ✅ Историю всех изменений (в папке `.git`)
- ✅ Все ветки проекта (можно переключиться командой `git branch -a`)

#### Проверка успешного клонирования

```powershell
# После клонирования выполните:
cd care-monitoring

# Проверьте наличие основных папок
Test-Path api-gateway        # Должно вернуть True
Test-Path microservices      # Должно вернуть True
Test-Path frontend           # Должно вернуть True
Test-Path package.json       # Должно вернуть True

# Посмотрите список файлов и папок
Get-ChildItem -Directory     # Покажет все папки
Get-ChildItem -File          # Покажет все файлы в корне

# Проверьте Git статус
git status                   # Покажет статус репозитория
git log --oneline -5         # Покажет последние 5 коммитов
```

**Или через SSH (если настроены SSH ключи):**

```powershell
# GitHub через SSH
git clone git@github.com:your-username/care-monitoring.git
cd care-monitoring

# GitLab через SSH
git clone git@gitlab.com:your-username/care-monitoring.git
cd care-monitoring
```

**Где найти URL репозитория:**
- **GitHub**: Откройте репозиторий → кнопка зеленого цвета "Code" → скопируйте HTTPS или SSH URL
- **GitLab**: Откройте репозиторий → кнопка "Clone" → скопируйте URL
- **Bitbucket**: Откройте репозиторий → кнопка "Clone" → скопируйте URL

**Пример полной команды для GitHub:**
```powershell
git clone https://github.com/your-org/care-monitoring.git
cd care-monitoring
```

#### Как файлы попадают в репозиторий?

**Важно понимать:** Репозиторий на сервере (GitHub/GitLab) создаётся и наполняется **до** того, как вы его клонируете. Разработчики проекта уже загрузили туда все файлы.

**Процесс разработки и загрузки файлов:**

1. **Разработчик создаёт проект локально:**
   ```
   C:\dev\care-monitoring\
   ├── api-gateway\
   ├── microservices\
   └── ... (файлы создаются локально)
   ```

2. **Разработчик инициализирует Git в проекте:**
   ```powershell
   cd C:\dev\care-monitoring
   git init                    # Создаёт репозиторий
   git add .                   # Добавляет все файлы (ВАЖНО: пробел между "add" и ".")
   git commit -m "Initial commit"  # Сохраняет изменения
   ```

3. **Разработчик создаёт репозиторий на GitHub/GitLab:**
   - Заходит на GitHub → создаёт новый репозиторий
   - Получает URL репозитория

4. **Разработчик отправляет файлы на сервер:**
   ```powershell
   git remote add origin https://github.com/username/care-monitoring.git
   git push -u origin main     # Отправляет все файлы на сервер
   ```

5. **Теперь файлы на сервере:**
   ```
   GitHub/GitLab сервер:
   └── care-monitoring репозиторий
       ├── api-gateway\
       ├── microservices\
       └── ... (все файлы проекта)
   ```

6. **Вы клонируете репозиторий:**
   ```powershell
   git clone https://github.com/username/care-monitoring.git
   # Все файлы скачиваются с сервера к вам на компьютер
   ```

**Визуализация процесса:**

```
ЭТАП 1: Разработка (локально у разработчика)
┌─────────────────────────────┐
│ Компьютер разработчика      │
│ C:\dev\care-monitoring\     │
│ ├── файлы созданы           │
│ └── git init, git add       │
└─────────────────────────────┘
            │
            │ git push
            ↓
ЭТАП 2: Загрузка на сервер
┌─────────────────────────────┐
│ GitHub/GitLab сервер        │
│ care-monitoring репозиторий │
│ ├── файлы загружены         │
│ └── доступны всем           │
└─────────────────────────────┘
            │
            │ git clone
            ↓
ЭТАП 3: Вы скачиваете проект
┌─────────────────────────────┐
│ Ваш компьютер               │
│ C:\projects\care-monitoring\│
│ ├── файлы склонированы      │
│ └── готовы к работе         │
└─────────────────────────────┘
```

#### Как обновлять склонированный проект?

Если разработчики добавили новые файлы или изменили существующие, вы можете обновить свой локальный проект:

```powershell
# Перейдите в папку проекта
cd C:\projects\care-monitoring

# Обновите проект (скачает новые/изменённые файлы)
git pull

# Или более явно:
git fetch origin    # Получить изменения
git merge origin/main   # Применить изменения
```

**Что происходит при обновлении:**
```
GitHub/GitLab (новые изменения)
         ↓
[git pull команда]
         ↓
Скачивание новых/изменённых файлов
         ↓
Ваш локальный проект обновлён
```

#### Создание нового репозитория (если проект только у вас)

Если вы хотите создать новый репозиторий из локальных файлов:

1. **Создайте проект локально:**
   ```powershell
   mkdir C:\projects\my-project
   cd C:\projects\my-project
   # Создайте файлы проекта
   ```

2. **Инициализируйте Git:**
   ```powershell
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. **Создайте репозиторий на GitHub:**
   - Зайдите на github.com
   - Нажмите "New repository"
   - Создайте репозиторий (без README)

4. **Отправьте файлы:**
   ```powershell
   git remote add origin https://github.com/your-username/my-project.git
   git branch -M main
   git push -u origin main
   ```

Теперь другие могут клонировать ваш репозиторий!

#### Загрузка существующих файлов в репозиторий

**Если у вас уже есть разработанный проект и вы хотите загрузить его в репозиторий:**

**Ваша ситуация:**
```
C:\projects\cursor_care-monitoring\
├── api-gateway\
├── microservices\
├── frontend\
└── ... (все файлы уже созданы)
```

**Шаг 1: Перейдите в папку проекта**

```powershell
cd C:\projects\cursor_care-monitoring
# Или туда, где находятся ваши файлы
```

**Шаг 2: Проверьте, есть ли уже Git репозиторий**

```powershell
# Проверка наличия .git папки
Test-Path .git

# Если True - Git уже инициализирован, переходите к шагу 4
# Если False - нужно инициализировать (шаг 3)
```

**Шаг 3: Инициализация Git (если ещё не сделано)**

```powershell
# Инициализация Git репозитория
git init

# Проверка статуса
git status
# Покажет все файлы, которые ещё не добавлены в Git
```

**Шаг 4: Настройка Git (первый раз, если не настроено)**

```powershell
# Укажите своё имя и email
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"

# Или глобально для всех репозиториев:
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

**Шаг 5: Добавление всех файлов в Git**

```powershell
# Добавить все файлы и папки
git add .

# Проверка того, что будет закоммичено
git status
# Вы увидите список файлов, готовых к коммиту (зелёным цветом)
```

**Шаг 6: Создание первого коммита**

```powershell
# Создать коммит с сообщением
git commit -m "Initial commit: полный проект Care Monitoring System"

# Проверка коммитов
git log --oneline
# Должен показать ваш коммит
```

**Шаг 7: Создание репозитория на GitHub/GitLab**

**Вариант A: GitHub**

1. Откройте [github.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `care-monitoring` (или другое имя)
   - **Description**: описание проекта (опционально)
   - **Visibility**: Public или Private
   - ⚠️ **НЕ отмечайте** "Add a README file" (у вас уже есть файлы)
   - ⚠️ **НЕ выбирайте** .gitignore или license (если они уже есть)
4. Нажмите **"Create repository"**
5. Скопируйте URL репозитория (он будет показан на следующей странице):
   - HTTPS: `https://github.com/your-username/care-monitoring.git`
   - Или SSH: `git@github.com:your-username/care-monitoring.git`

**Вариант B: GitLab**

1. Откройте [gitlab.com](https://gitlab.com) и войдите в аккаунт
2. Нажмите **"New project"** или **"+"** → **"New project/repository"**
3. Выберите **"Create blank project"**
4. Заполните:
   - **Project name**: `care-monitoring`
   - **Visibility**: Public, Internal или Private
5. Нажмите **"Create project"**
6. Скопируйте URL репозитория

**Шаг 8: Подключение локального репозитория к удалённому**

```powershell
# Добавить удалённый репозиторий (замените URL на ваш)
git remote add origin https://github.com/your-username/care-monitoring.git

# Проверка подключения
git remote -v
# Должно показать:
# origin  https://github.com/your-username/care-monitoring.git (fetch)
# origin  https://github.com/your-username/care-monitoring.git (push)
```

**Шаг 9: Переименование ветки (если нужно)**

```powershell
# Убедитесь, что вы на главной ветке
git branch

# Если ветка называется не "main", переименуйте:
git branch -M main
# Или если нужно "master":
# git branch -M master
```

**Шаг 10: Отправка файлов на сервер**

```powershell
# Отправить все файлы на сервер
git push -u origin main

# Если используете ветку master:
# git push -u origin master
```

**Что вы увидите:**
```
Enumerating objects: 1234, done.
Counting objects: 100% (1234/1234), done.
Delta compression using up to 8 threads
Compressing objects: 100% (890/890), done.
Writing objects: 100% (1234/1234), 5.67 MiB | 2.45 MiB/s, done.
Total 1234 (delta 344), reused 0 (delta 0), pack-reused 0
remote: Resolving deltas: 100% (344/344), done.
To https://github.com/your-username/care-monitoring.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

**Шаг 11: Проверка результата**

1. Откройте ваш репозиторий на GitHub/GitLab в браузере
2. Вы должны увидеть все ваши файлы:
   - `api-gateway/`
   - `microservices/`
   - `frontend/`
   - `package.json`
   - `README.md`
   - и т.д.

**Готово! Теперь ваш проект доступен в репозитории.**

#### Полный пример команды для загрузки существующего проекта

```powershell
# 1. Перейти в папку проекта
cd C:\projects\cursor_care-monitoring

# 2. Инициализировать Git (если ещё не сделано)
git init

# 3. Настроить Git (если первый раз)
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"

# 4. Добавить все файлы
git add .

# 5. Создать коммит
git commit -m "Initial commit: Care Monitoring System"

# 6. Создать репозиторий на GitHub (в браузере)
# Затем скопировать URL, например: https://github.com/username/care-monitoring.git

# 7. Подключить удалённый репозиторий
git remote add origin https://github.com/your-username/care-monitoring.git

# 8. Переименовать ветку в main (если нужно)
git branch -M main

# 9. Отправить файлы
git push -u origin main
```

#### Что делать, если репозиторий уже существует?

Если на GitHub/GitLab уже создан репозиторий с файлами (например, с README):

```powershell
# Подключить существующий репозиторий
git remote add origin https://github.com/your-username/care-monitoring.git

# Получить изменения с сервера
git fetch origin

# Объединить с вашими изменениями
git merge origin/main --allow-unrelated-histories

# Отправить ваши файлы
git push -u origin main
```

#### Исключение файлов из Git (.gitignore)

Если есть файлы, которые не нужно загружать (node_modules, .env и т.д.), создайте `.gitignore`:

```powershell
# Создать .gitignore в корне проекта
New-Item .gitignore

# Открыть для редактирования
notepad .gitignore
```

**Содержимое .gitignore:**
```
# Зависимости
node_modules/
package-lock.json

# Переменные окружения
.env
.env.local
.env.*.local

# Логи
logs/
*.log

# Временные файлы
.DS_Store
Thumbs.db
*.swp
*.swo

# Build файлы
dist/
build/
*.tsbuildinfo

# IDE
.vscode/
.idea/
*.iml
```

Затем:
```powershell
git add .gitignore
git commit -m "Add .gitignore"
git push
```

#### Резюме

- ✅ **Существующие файлы** уже находятся на сервере (GitHub/GitLab)
- ✅ **Разработчики** загрузили их туда ранее через `git push`
- ✅ **Вы клонируете** их на свой компьютер через `git clone`
- ✅ **Обновления** получаете через `git pull`
- ✅ **Новые файлы** можно загрузить через `git add` → `git commit` → `git push`
- ✅ **Загрузка существующего проекта**: `git init` → `git add .` → `git commit` → создать репозиторий → `git remote add` → `git push`

### 2. Настройка PowerShell Execution Policy

Если при выполнении скриптов возникает ошибка "execution of scripts is disabled", выполните:

```powershell
# Откройте PowerShell от имени администратора
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Или для одного сеанса:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/install-all.ps1
```

### 3. Настройка ExecutionPolicy для PowerShell

**Важно:** На Windows PowerShell может блокировать выполнение скриптов. Нужно разрешить выполнение:

**Вариант 1: Временно для текущей сессии (рекомендуется для начала)**

```powershell
# Разрешить выполнение скриптов для текущего пользователя
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# При запросе подтверждения нажмите Y (Yes)
```

**Вариант 2: Только для текущего процесса**

```powershell
# Запуск PowerShell с обходом политики (для одной команды)
powershell -ExecutionPolicy Bypass -Command "ваша-команда"
```

**Вариант 3: Использовать cmd.exe вместо PowerShell**

```cmd
# В командной строке (cmd.exe) политика выполнения не применяется
cmd
npm install
```

### 4. Установка npm зависимостей

**Важно:** Проект использует `file:` протокол вместо workspaces для лучшей совместимости.

**Вариант A: Автоматическая установка (рекомендуется)**

```powershell
# С обходом ExecutionPolicy (если не настроена)
powershell -ExecutionPolicy Bypass -File scripts/install-all.ps1

# Или если ExecutionPolicy уже настроена:
.\scripts\install-all.ps1
```

**Вариант B: Через npm скрипт**

```powershell
# Из корня проекта (с обходом ExecutionPolicy если нужно)
powershell -ExecutionPolicy Bypass -Command "npm run install:all"

# Или если ExecutionPolicy настроена:
npm run install:all
```

**Вариант C: Ручная установка**

```powershell
# 1. Сначала установите shared пакет (обязательно!)
cd shared
powershell -ExecutionPolicy Bypass -Command "npm install"
cd ..

# 2. Установите realtime пакет
cd frontend/packages/realtime
powershell -ExecutionPolicy Bypass -Command "npm install"
cd ../../..

# 3. Установите api-gateway
cd api-gateway
powershell -ExecutionPolicy Bypass -Command "npm install"
cd ..

# 4. Установите каждый микросервис
$services = @(
    "auth-service", "user-service", "device-service", 
    "telemetry-service", "alert-service", "location-service",
    "billing-service", "integration-service", "dispatcher-service",
    "analytics-service", "ai-prediction-service", "organization-service"
)

foreach ($service in $services) {
    Write-Host "Installing $service..." -ForegroundColor Cyan
    Set-Location "microservices/$service"
    powershell -ExecutionPolicy Bypass -Command "npm install"
    Set-Location ../..
}

# 5. Установите frontend приложения
$apps = @("guardian-app", "admin-app", "dispatcher-app", "landing-app")

foreach ($app in $apps) {
    Write-Host "Installing $app..." -ForegroundColor Cyan
    Set-Location "frontend/apps/$app"
    powershell -ExecutionPolicy Bypass -Command "npm install"
    Set-Location ../../..
}
```

**Время установки:** 10-20 минут (зависит от скорости интернета)

**Примечание:** Каждый сервис имеет свои `node_modules`, что обеспечивает изоляцию зависимостей.

---

## Настройка окружения

### 1. Создание файлов .env

**PowerShell скрипт для автоматического копирования:**

```powershell
# Корневой .env (если существует)
if (Test-Path .env.example) {
    Copy-Item .env.example .env
}

# Инфраструктура
if (Test-Path infrastructure/env.example) {
    Copy-Item infrastructure/env.example infrastructure/.env
}

# API Gateway
if (Test-Path api-gateway/env.example) {
    Copy-Item api-gateway/env.example api-gateway/.env
}

# Микросервисы
$services = @(
    "auth-service", "user-service", "device-service", 
    "telemetry-service", "alert-service", "location-service",
    "billing-service", "integration-service", "dispatcher-service",
    "analytics-service", "ai-prediction-service", "organization-service"
)

foreach ($service in $services) {
    $examplePath = "microservices/$service/env.example"
    $envPath = "microservices/$service/.env"
    if (Test-Path $examplePath) {
        Copy-Item $examplePath $envPath
        Write-Host "Created .env for $service" -ForegroundColor Green
    }
}

# Frontend приложения
$apps = @("guardian-app", "admin-app", "dispatcher-app", "landing-app")

foreach ($app in $apps) {
    $examplePath = "frontend/apps/$app/env.example"
    $envPath = "frontend/apps/$app/.env"
    if (Test-Path $examplePath) {
        Copy-Item $examplePath $envPath
        Write-Host "Created .env for $app" -ForegroundColor Green
    }
}

Write-Host "All .env files created successfully!" -ForegroundColor Green
```

**Или вручную:**

```powershell
# Инфраструктура
Copy-Item infrastructure/env.example infrastructure/.env

# API Gateway
Copy-Item api-gateway/env.example api-gateway/.env

# Микросервисы (пример для одного)
Copy-Item microservices/auth-service/env.example microservices/auth-service/.env
# Повторите для всех сервисов
```

### 2. Настройка переменных окружения

Отредактируйте `.env` файлы с вашими настройками:

```powershell
# Открыть в Notepad
notepad infrastructure/.env

# Или в VS Code
code infrastructure/.env

# Или в другом редакторе
```

**Минимальные настройки для development (`infrastructure/.env`):**

```env
# PostgreSQL Configuration
POSTGRES_USER=cms_user
POSTGRES_PASSWORD=cms_password
POSTGRES_DB=care_monitoring
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379
REDIS_PASSWORD=

# RabbitMQ Configuration
RABBITMQ_DEFAULT_USER=cms
RABBITMQ_DEFAULT_PASS=cms
RABBITMQ_DEFAULT_VHOST=/
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# Network Configuration
NETWORK_NAME=care-monitoring-network

# Environment
ENVIRONMENT=development
```

---

## Проверка установки

### 1. Проверка версий

```powershell
# Node.js и npm
node --version  # Должно быть v18.x.x или выше
npm --version   # Должно быть 9.x.x или выше

# Docker
docker --version
docker compose version

# Git
git --version

# PowerShell версия
$PSVersionTable.PSVersion
```

### 2. Проверка структуры проекта

```powershell
# Проверка директорий
Get-ChildItem -Directory | Select-Object Name

# Должны быть видны:
# - api-gateway/
# - microservices/
# - shared/
# - frontend/
# - infrastructure/
# - node_modules/
# - package.json

# Проверка установленных зависимостей в корне
Test-Path node_modules
```

### 3. Проверка Docker

```powershell
# Запуск тестового контейнера
docker run hello-world

# Проверка Docker Compose
docker compose version

# Проверка статуса Docker Desktop
docker ps
```

### 4. Проверка установленных зависимостей сервисов

```powershell
# Проверка нескольких сервисов
Test-Path api-gateway/node_modules
Test-Path shared/node_modules
Test-Path microservices/auth-service/node_modules
```

---

## Следующие шаги

После успешной установки:

### 1. Запустите инфраструктуру

```powershell
# Из корня проекта
npm run dev:infra

# Или вручную
cd infrastructure
docker compose up -d
cd ..
```

**Проверка:**

```powershell
docker compose -f infrastructure/docker-compose.yml ps
```

Должны быть запущены: PostgreSQL, Redis, RabbitMQ

### 2. Примените миграции базы данных

```powershell
npm run db:migrate
```

### 3. Запустите все сервисы

```powershell
# Запуск всех сервисов одновременно
npm run dev:all
```

**Или по отдельности в разных окнах PowerShell:**

```powershell
# Окно 1: API Gateway
npm run dev:gateway

# Окно 2: Микросервисы
npm run dev:services

# Окно 3: Frontend приложения
npm run dev:frontend
```

### 4. Запуск отдельных frontend приложений

```powershell
# Guardian App (http://localhost:5173)
cd frontend/apps/guardian-app
npm run dev

# Dispatcher App (http://localhost:5174)
cd frontend/apps/dispatcher-app
npm run dev

# Admin App (http://localhost:5185)
cd frontend/apps/admin-app
npm run dev

# Landing App (http://localhost:5175)
cd frontend/apps/landing-app
npm run dev
```

**Подробная инструкция:** [DEPLOYMENT.md](DEPLOYMENT.md)

---

## Troubleshooting

### Проблемы с Node.js

**Ошибка: "node: command not found" или "node is not recognized"**

```powershell
# Проверьте установку
Get-Command node -ErrorAction SilentlyContinue

# Если не найдено:
# 1. Перезапустите PowerShell
# 2. Проверьте PATH: $env:PATH -split ';' | Select-String node
# 3. Переустановите Node.js, убедившись что опция "Add to PATH" включена
```

**Неверная версия Node.js**

Используйте nvm-windows для управления версиями:

1. Скачайте [nvm-windows](https://github.com/coreybutler/nvm-windows/releases)
2. Установите nvm-windows
3. Используйте:

```powershell
nvm install 18
nvm use 18
node --version
```

### Проблемы с Docker

**Ошибка: "Cannot connect to Docker daemon" или "Docker daemon is not running"**

```powershell
# 1. Убедитесь, что Docker Desktop запущен (иконка в системном трее)
# 2. Перезапустите Docker Desktop
# 3. Проверьте статус
docker ps

# 4. Если проблемы с WSL2:
wsl --list --verbose
wsl --update
```

**Ошибка: "WSL 2 installation is incomplete"**

```powershell
# Установите WSL2
wsl --install

# Установите обновление WSL2 вручную:
# https://aka.ms/wsl2kernel

# Перезапустите компьютер
```

**Порт уже занят**

```powershell
# Проверка занятых портов
netstat -ano | findstr :5432  # PostgreSQL
netstat -ano | findstr :6379  # Redis
netstat -ano | findstr :5672  # RabbitMQ

# Остановка процесса на порту (замените PID)
taskkill /PID <PID> /F
```

### Проблемы с PowerShell

**Ошибка: "execution of scripts is disabled on this system" или "Невозможно загрузить файл npm.ps1"**

```powershell
# РЕШЕНИЕ 1: Настроить ExecutionPolicy (рекомендуется)
# Откройте PowerShell от имени администратора и выполните:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# При запросе подтверждения нажмите Y (Yes)

# РЕШЕНИЕ 2: Для одного запуска скрипта:
powershell -ExecutionPolicy Bypass -File scripts/install-all.ps1

# РЕШЕНИЕ 3: Для команды npm:
powershell -ExecutionPolicy Bypass -Command "npm install"

# РЕШЕНИЕ 4: Использовать cmd.exe (не требует ExecutionPolicy):
cmd
npm install
```

**Ошибка: "ParserError: MissingEndParenthesisInMethodCall"**

Используйте чистый PowerShell:

```powershell
powershell -NoProfile
# Затем выполните команды
```

### Проблемы с npm

**Ошибка: "execution of scripts is disabled" или "Невозможно загрузить файл npm.ps1"**

```powershell
# РЕШЕНИЕ 1: Настроить ExecutionPolicy (рекомендуется)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# РЕШЕНИЕ 2: Использовать обход для одной команды
powershell -ExecutionPolicy Bypass -Command "npm install"

# РЕШЕНИЕ 3: Использовать cmd.exe (не требует ExecutionPolicy)
cmd
npm install
```

**Ошибка: "EACCES: permission denied"**

```powershell
# Обычно не проблема на Windows, но если возникает:
# 1. Запустите PowerShell от имени администратора
# 2. Или измените директорию npm cache:
npm config set cache "C:\npm-cache" --global
```

**Медленная установка пакетов**

```powershell
# Используйте зеркало (если основное медленное)
npm config set registry https://registry.npmmirror.com

# Увеличьте таймаут
npm config set fetch-timeout 60000
npm config set fetch-retry-mintimeout 20000

# Очистка кэша
npm cache clean --force
```

**Ошибка: "EPERM: operation not permitted"**

```powershell
# Закройте все процессы, использующие node_modules
# Или выполните:
Remove-Item -Recurse -Force node_modules
npm install
```

### Проблемы с зависимостями

**Ошибка: "Module not found"**

```powershell
# Очистка и переустановка (с обходом ExecutionPolicy)
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item package-lock.json -ErrorAction SilentlyContinue
powershell -ExecutionPolicy Bypass -Command "npm install"
```

**Конфликты версий**

```powershell
# Обновление npm
powershell -ExecutionPolicy Bypass -Command "npm install -g npm@latest"

# Очистка кэша
powershell -ExecutionPolicy Bypass -Command "npm cache clean --force"

# Переустановка зависимостей
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
powershell -ExecutionPolicy Bypass -Command "npm install"
```

**Ошибка установки native модулей**

Убедитесь, что установлены инструменты сборки:

```powershell
# Через Chocolatey
choco install visualstudio2019buildtools
choco install python3

# Или установите Visual Studio Build Tools вручную:
# https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022
```

### Проблемы с Docker Compose

**Предупреждение: "the attribute `version` is obsolete"**

Это нормально. В новых версиях Docker Compose атрибут `version` не требуется и игнорируется.

**Ошибка: "no configuration file provided: not found"**

```powershell
# Убедитесь, что вы в правильной директории
cd infrastructure
docker compose up -d
```

### Проблемы с путями

**Ошибка: "Path too long"**

Windows имеет ограничение на длину пути (260 символов). Решения:

1. Включите длинные пути в Windows:
   ```powershell
   # В PowerShell от имени администратора
   New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `
     -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
   ```

2. Или используйте короткие пути:
   ```powershell
   # Установите проект ближе к корню диска
   # Например: C:\dev\care-monitoring
   ```

---

## Дополнительная помощь

- [DEPLOYMENT.md](DEPLOYMENT.md) - Руководство по развертыванию
- [DOCKER_DEPLOYMENT.md](DOCKER_DEPLOYMENT.md) - Развертывание в Docker
- [README.md](README.md) - Общая информация
- [DEVELOPMENT.md](DEVELOPMENT.md) - Руководство по разработке
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем

---

**Последнее обновление:** 2025-12-08
