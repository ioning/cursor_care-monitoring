# Настройка карт и графиков

## Установка зависимостей

### 1. Установка npm пакетов

```bash
cd mobile/ward-app
npm install
```

### 2. Настройка для Android

#### Получение Google Maps API ключа

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект или выберите существующий
3. Включите **Maps SDK for Android** и **Maps SDK for iOS**
4. Создайте API ключ в разделе "Credentials"
5. Ограничьте ключ (рекомендуется):
   - Для Android: ограничьте по package name и SHA-1 fingerprint
   - Для iOS: ограничьте по Bundle ID

#### Настройка AndroidManifest.xml

Откройте `android/app/src/main/AndroidManifest.xml` и замените `YOUR_GOOGLE_MAPS_API_KEY` на ваш API ключ:

```xml
<meta-data
    android:name="com.google.android.geo.API_KEY"
    android:value="ВАШ_GOOGLE_MAPS_API_KEY"/>
```

#### Получение SHA-1 fingerprint (для ограничения API ключа)

**Debug keystore:**
```bash
cd android/app
keytool -list -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android
```

**Release keystore:**
```bash
keytool -list -v -keystore release.keystore -alias YOUR_KEY_ALIAS
```

### 3. Настройка для iOS

#### Настройка Info.plist

Убедитесь, что в `ios/CareMonitoringWard/Info.plist` есть описание использования геолокации:

```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>Приложению требуется доступ к геолокации для отображения местоположения подопечных на карте</string>
```

#### Настройка AppDelegate

Для использования Google Maps на iOS добавьте импорт в `ios/CareMonitoringWard/AppDelegate.mm`:

```objc
#import <GoogleMaps/GoogleMaps.h>
```

И инициализируйте в методе `didFinishLaunchingWithOptions`:

```objc
[GMSServices provideAPIKey:@"ВАШ_GOOGLE_MAPS_API_KEY"];
```

**Альтернатива:** Если вы хотите использовать Apple Maps вместо Google Maps (требуется только на iOS), измените `PROVIDER_GOOGLE` на `PROVIDER_DEFAULT` в `GuardianDashboardScreen.tsx`:

```typescript
<MapView
  provider={PROVIDER_DEFAULT}  // Использует Apple Maps
  ...
/>
```

#### Установка CocoaPods зависимостей

```bash
cd ios
pod install
cd ..
```

## Проверка установки

### Android

1. Запустите приложение: `npm run android`
2. Перейдите на экран дашборда опекуна
3. Карта должна отображаться с маркерами подопечных

### iOS

1. Запустите приложение: `npm run ios`
2. Перейдите на экран дашборда опекуна
3. Карта должна отображаться с маркерами подопечных

## Использование

### Графики телеметрии

Графики автоматически отображаются на экране `TelemetryHistoryScreen` при наличии данных.

**Периоды:**
- 1 час
- 6 часов
- 24 часа
- 7 дней
- 30 дней

### Карты на дашборде опекуна

Карта автоматически отображает:
- Маркеры для каждого подопечного
- Цвета маркеров в зависимости от статуса:
  - 🟢 Зеленый - Стабильно
  - 🟠 Оранжевый - Нужно внимание
  - 🔴 Красный - Тревога

**Интерактивность:**
- Нажмите на маркер, чтобы увидеть имя подопечного и адрес
- Карта автоматически центрируется на первом подопечном

## Устранение проблем

### Android: Карта не отображается

1. Проверьте, что API ключ правильно указан в `AndroidManifest.xml`
2. Убедитесь, что **Maps SDK for Android** включен в Google Cloud Console
3. Проверьте, что SHA-1 fingerprint добавлен в ограничения API ключа
4. Очистите кэш: `cd android && ./gradlew clean`

### iOS: Карта не отображается

1. Проверьте, что API ключ правильно указан в `AppDelegate.mm`
2. Убедитесь, что **Maps SDK for iOS** включен в Google Cloud Console
3. Проверьте, что Bundle ID добавлен в ограничения API ключа
4. Переустановите pods: `cd ios && pod deintegrate && pod install`

### Графики не отображаются

1. Убедитесь, что `react-native-svg` установлен: `npm list react-native-svg`
2. Для Android: `cd android && ./gradlew clean`
3. Для iOS: `cd ios && pod install`
4. Перезапустите Metro bundler: `npm start -- --reset-cache`

## Дополнительные настройки

### Кастомизация графиков

Измените стиль графиков в `TelemetryHistoryScreen.tsx` в объекте `chartConfig`:

```typescript
chartConfig={{
  backgroundColor: colors.surface,
  color: (opacity = 1) => colors.primary,
  // ... другие настройки
}}
```

### Кастомизация карты

Измените стиль карты в `GuardianDashboardScreen.tsx`:

```typescript
<MapView
  mapType="satellite"  // или "standard", "hybrid"
  showsCompass={true}
  showsScale={true}
  // ... другие настройки
/>
```

### Регионы для карты

Настройте автоматическое определение региона на основе местоположений подопечных:

```typescript
// Вычислите bounding box для всех местоположений
const calculateRegion = (wards: WardStatus[]) => {
  const locations = wards.filter(w => w.location).map(w => w.location!);
  // ... логика вычисления
};
```

## Безопасность API ключей

⚠️ **Важно:** Не коммитьте API ключи в репозиторий!

1. Используйте переменные окружения для API ключей
2. Добавьте `.env` в `.gitignore`
3. Используйте разные ключи для development и production
4. Ограничьте API ключи по package name / Bundle ID и SHA-1

## Дополнительные ресурсы

- [react-native-maps документация](https://github.com/react-native-maps/react-native-maps)
- [react-native-chart-kit документация](https://github.com/indiespirit/react-native-chart-kit)
- [Google Maps Platform](https://developers.google.com/maps)

