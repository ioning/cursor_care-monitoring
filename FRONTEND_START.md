# Инструкция по запуску Frontend приложений

## Проблема: На портах 5173 и 5174 показывается кабинет опекуна

### Решение

**Важно:** Каждое приложение должно запускаться отдельно в своем терминале!

## Способ 1: Запуск через npm скрипты (рекомендуется)

### В отдельных окнах PowerShell:

**Окно 1 - Guardian App (порт 5173):**
```powershell
cd frontend/apps/guardian-app
npm run dev
```

**Окно 2 - Dispatcher App (порт 5174):**
```powershell
cd frontend/apps/dispatcher-app
npm run dev
```

### Или из корня проекта:

**Окно 1:**
```powershell
npm run dev:guardian
```

**Окно 2:**
```powershell
npm run dev:dispatcher
```

## Способ 2: Использование скрипта

```powershell
# Из корня проекта
.\scripts\start-frontend.ps1
```

## Проверка портов

Убедитесь, что порты настроены правильно:

- **Guardian App**: `frontend/apps/guardian-app/vite.config.ts` → `port: 5173`
- **Dispatcher App**: `frontend/apps/dispatcher-app/vite.config.ts` → `port: 5174`

## Как отличить приложения

### Guardian App (порт 5173):
- Заголовок: "Care Monitoring"
- Фон: Темно-синий/фиолетовый градиент
- Навигация: Дашборд, Подопечные, Устройства, Алерты

### Dispatcher App (порт 5174):
- Заголовок: "🚨 Dispatcher"
- Фон: Темно-красный градиент
- Навигация: Панель, Вызовы, Карта

## Если проблема сохраняется

1. **Остановите все запущенные приложения** (Ctrl+C во всех окнах)
2. **Проверьте занятость портов:**
   ```powershell
   Get-NetTCPConnection -LocalPort 5173 -ErrorAction SilentlyContinue
   Get-NetTCPConnection -LocalPort 5174 -ErrorAction SilentlyContinue
   ```
3. **Очистите кеш браузера** (Ctrl+Shift+Delete)
4. **Перезапустите приложения** по инструкции выше

## Важно

❌ **НЕ используйте** `npm run dev:frontend` если он запускает все приложения одновременно - это может вызвать конфликты портов.

✅ **Используйте** отдельные окна для каждого приложения.

