# Проверка выхода данных из мобильного приложения

## Дата проверки: 2025-12-25

## Резюме

Проведена проверка выхода данных из мобильного приложения (ward-app) для обеспечения полного потока всех необходимых данных от браслета и телефона.

## ✅ Текущее состояние

### 1. Данные телеметрии от браслета

**Мобильное приложение отправляет:**
```typescript
{
  deviceId?: string;
  wardId?: string;
  metricType: string;  // например, "heart_rate"
  value: number;
  unit: string;        // например, "bpm"
  timestamp: string;
}
```

**Отправка:** `POST /api/v1/telemetry`

**Источник данных:**
- BluetoothService получает данные от браслета через BLE
- Парсит JSON из characteristic.value
- Отправляет каждую метрику отдельно через TelemetryService.sendTelemetry()

**Проблема:** Мобильное приложение отправляет метрики по одной, а не пакетом. Это может быть неэффективно, но работает благодаря адаптеру в API Gateway.

### 2. Данные локации от телефона

**Мобильное приложение отправляет:**
```typescript
{
  latitude: number;
  longitude: number;
  accuracy?: number;
  source: 'mobile_app';
}
```

**Отправка:** `POST /api/v1/locations/wards/:wardId`

**Источник данных:**
- LocationService использует Geolocation.watchPosition()
- Отправляет локацию при изменении позиции (distanceFilter: 10 метров)
- Отправляется отдельно от телеметрии

**Проблема:** Локация отправляется отдельно от телеметрии, хотя в документации указано, что она может быть включена в запрос телеметрии.

## ⚠️ Обнаруженные проблемы

### 1. Отсутствие qualityScore

**Проблема:** Мобильное приложение не отправляет `qualityScore` для метрик.

**Ожидается:**
```json
{
  "type": "heart_rate",
  "value": 72,
  "unit": "bpm",
  "qualityScore": 0.95,  // ← Отсутствует
  "timestamp": "2025-12-25T10:30:00.000Z"
}
```

**Текущее состояние:**
- BluetoothService.handleTelemetryData() не извлекает qualityScore из данных браслета
- TelemetryService.sendTelemetry() не включает qualityScore

**Рекомендация:** Добавить поддержку qualityScore в BluetoothService и TelemetryService.

### 2. Отсутствие данных акселерометра

**Проблема:** Мобильное приложение не отправляет данные акселерометра (accel_x, accel_y, accel_z).

**Ожидается (согласно документации):**
```json
{
  "type": "accelerometer",
  "value": {
    "x": 0.5,
    "y": -0.3,
    "z": 9.8
  },
  "unit": "g",
  "qualityScore": 0.90,
  "timestamp": "2025-12-25T10:30:00.000Z"
}
```

**Текущее состояние:**
- BluetoothService подписывается только на Heart Rate Measurement (characteristicUUID: '00002a37-0000-1000-8000-00805f9b34fb')
- Не подписывается на акселерометр или другие характеристики
- Не отправляет данные акселерометра

**Рекомендация:** Добавить подписку на акселерометр и отправку данных акселерометра.

### 3. Отсутствие объединения метрик в пакет

**Проблема:** Мобильное приложение отправляет каждую метрику отдельно, а не пакетом.

**Ожидается (согласно документации):**
```json
{
  "deviceId": "...",
  "metrics": [
    { "type": "heart_rate", "value": 72, ... },
    { "type": "spo2", "value": 98, ... },
    { "type": "temperature", "value": 36.6, ... },
    { "type": "steps", "value": 1240, ... },
    { "type": "battery", "value": 85, ... }
  ],
  "location": { ... }
}
```

**Текущее состояние:**
- Каждая метрика отправляется отдельным запросом
- Адаптер в API Gateway преобразует формат, но это неэффективно

**Рекомендация:** Добавить буферизацию метрик и отправку пакетом.

### 4. Отсутствие объединения локации с телеметрией

**Проблема:** Локация отправляется отдельно от телеметрии, хотя может быть включена в запрос телеметрии.

**Ожидается (согласно документации):**
```json
{
  "deviceId": "...",
  "metrics": [...],
  "location": {
    "latitude": 55.7558,
    "longitude": 37.6173,
    "accuracy": 10,
    "source": "gps"
  }
}
```

**Текущее состояние:**
- Локация отправляется отдельно через `/locations/wards/:wardId`
- Телеметрия отправляется через `/telemetry` без локации

**Рекомендация:** Добавить возможность включения локации в запрос телеметрии.

## 📊 Текущий поток данных

### Телеметрия (от браслета)
```
Браслет (BLE)
  ↓ characteristic.value (JSON)
BluetoothService.handleTelemetryData()
  ↓ Парсинг JSON
  ↓ { type, value, unit, timestamp }
TelemetryService.sendTelemetry()
  ↓ POST /api/v1/telemetry
  ↓ { deviceId, metricType, value, unit, timestamp }
API Gateway
  ↓ Преобразование формата
  ↓ { deviceId, metrics: [{ type, value, unit, timestamp }] }
TelemetryService
  ↓ Сохранение в БД
  ↓ Отправка локации в LocationService (если есть)
  ↓ Создание алертов
```

### Локация (от телефона)
```
Телефон (GPS/Network)
  ↓ Geolocation.watchPosition()
LocationService.startTracking()
  ↓ { latitude, longitude, accuracy }
ApiLocationService.sendLocation()
  ↓ POST /api/v1/locations/wards/:wardId
  ↓ { latitude, longitude, accuracy, source: 'mobile_app' }
LocationService
  ↓ Сохранение в БД
  ↓ Проверка геозон
  ↓ Создание алертов
```

## ✅ Что работает правильно

1. **Отправка телеметрии:** Мобильное приложение отправляет данные от браслета
2. **Отправка локации:** Мобильное приложение отправляет локацию от телефона
3. **Адаптер формата:** API Gateway преобразует формат данных
4. **Офлайн режим:** Данные сохраняются в очередь при отсутствии сети
5. **JWT аутентификация:** Все запросы используют JWT токен

## ⚠️ Что требует улучшения

1. **qualityScore:** Добавить поддержку qualityScore для метрик
2. **Акселерометр:** Добавить подписку на акселерометр и отправку данных
3. **Пакетная отправка:** Отправлять метрики пакетом, а не по одной
4. **Объединение данных:** Включать локацию в запрос телеметрии
5. **Подписка на все характеристики:** Подписаться на все доступные характеристики браслета

## 📝 Рекомендации

### 1. Улучшить BluetoothService

```typescript
// Добавить подписку на все характеристики
- Heart Rate (уже есть)
- Battery Level
- Accelerometer (x, y, z)
- Steps
- Temperature
- SpO2
```

### 2. Добавить буферизацию метрик

```typescript
// Собирать метрики в буфер
// Отправлять пакетом каждые N секунд или при достижении лимита
```

### 3. Включить локацию в телеметрию

```typescript
// При отправке телеметрии включать текущую локацию
TelemetryService.sendTelemetry({
  deviceId,
  metrics: [...],
  location: currentLocation  // ← Добавить
})
```

### 4. Добавить qualityScore

```typescript
// Извлекать qualityScore из данных браслета
// Или вычислять на основе качества сигнала BLE
```

## ✅ Оптимизации (реализовано)

### 1. Буферизация метрик
- ✅ Метрики собираются в буфер
- ✅ Отправка пакетом каждые 15 секунд или при заполнении буфера (10 метрик)
- ✅ Автоматическая отправка при отключении устройства

### 2. Поддержка qualityScore
- ✅ Извлекается из данных браслета (если доступно)
- ✅ Вычисляется на основе RSSI сигнала BLE
- ✅ Включается в каждую метрику

### 3. Подписка на все характеристики
- ✅ Heart Rate (0x2A37)
- ✅ Battery Level (0x2A19)
- ✅ Temperature (0x2A6D)
- ✅ Steps (0x2A53)
- ✅ SpO2 (0x2A5F)
- ✅ Accelerometer (если доступен)

### 4. Объединение локации с телеметрией
- ✅ Локация автоматически включается в запрос телеметрии
- ✅ Получается из Redux store
- ✅ Отправляется вместе с метриками пакетом

### 5. Оптимизированный формат данных
- ✅ Отправка в формате `{ deviceId, metrics: [...], location: {...} }`
- ✅ Соответствует ожидаемому формату API
- ✅ Минимизирует количество запросов

## ✅ Заключение

Основной поток данных оптимизирован:
- ✅ Отправка телеметрии работает (пакетом)
- ✅ Отправка локации работает (включена в телеметрию)
- ✅ qualityScore поддерживается
- ✅ Подписка на все характеристики браслета
- ✅ Метрики отправляются пакетом
- ✅ Локация включена в запрос телеметрии

Мобильное приложение оптимизировано для эффективной отправки всех необходимых данных.

