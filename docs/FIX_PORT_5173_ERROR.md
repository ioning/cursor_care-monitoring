# 🔧 Исправление ошибки "ERR_CONNECTION_REFUSED" на порту 5173

## Проблема:
Приложение пытается загрузить модули с порта 5173, но приложение теперь работает на другом порту.

## Решение:

### 1. **Закройте все старые вкладки браузера**
   - Закройте все вкладки с `localhost:5173`
   - Закройте все вкладки с `localhost:5174` (если это dispatcher-app)

### 2. **Очистите кеш браузера**
   
   **Chrome/Edge:**
   - Нажмите `Ctrl+Shift+Delete` (Windows) или `Cmd+Shift+Delete` (Mac)
   - Выберите "Кешированные изображения и файлы"
   - Нажмите "Удалить данные"

   **Или используйте жесткую перезагрузку:**
   - Нажмите `Ctrl+Shift+R` (Windows) или `Cmd+Shift+R` (Mac)

### 3. **Перезапустите приложения**

```bash
# Остановите все запущенные приложения (Ctrl+C в каждом терминале)

# Затем запустите заново:
npm run dev:guardian   # Порт 5174
npm run dev:dispatcher # Порт 5175
```

### 4. **Откройте правильные URL**

- **Guardian App**: http://localhost:5174 (НЕ 5173!)
- **Dispatcher App**: http://localhost:5175 (НЕ 5174!)

### 5. **Если проблема сохраняется:**

1. **Очистите localStorage:**
   - Откройте DevTools (F12)
   - Перейдите в Application → Local Storage
   - Удалите все записи для `localhost:5173` и `localhost:5174`

2. **Очистите Service Workers:**
   - DevTools → Application → Service Workers
   - Нажмите "Unregister" для всех зарегистрированных workers

3. **Перезапустите браузер полностью**

## Проверка:

После выполнения всех шагов:
- ✅ Guardian App должен открываться на http://localhost:5174
- ✅ Dispatcher App должен открываться на http://localhost:5175
- ✅ Не должно быть ошибок о порте 5173

## Если ничего не помогает:

1. Используйте режим инкогнито браузера
2. Или используйте другой браузер для проверки
3. Проверьте, что приложения действительно запущены на правильных портах:
   ```bash
   # Windows PowerShell:
   Get-NetTCPConnection -LocalPort 5174
   Get-NetTCPConnection -LocalPort 5175
   ```

