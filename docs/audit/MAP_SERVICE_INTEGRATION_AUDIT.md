# Аудит интеграции картографического сервиса

## 📋 Сводка

Проверка интеграции картографических и геокодинг сервисов в проекте Care Monitoring System.

**Дата проверки:** 2024-12-08  
**Статус:** 🟡 **Частично реализовано** - отсутствует геокодинг на backend

---

## ✅ Что реализовано

### 1. Frontend - Отображение карт

#### Guardian App & Dispatcher App
- ✅ **Leaflet** интегрирован для отображения карт
- ✅ **OpenStreetMap** используется как источник тайлов (бесплатно, без API ключей)
- ✅ Компоненты карт реализованы:
  - `WardLocationMap.vue` - карта для отдельного подопечного
  - `WardsLocationMap.vue` - карта всех подопечных
- ✅ Отображение маркеров, геозон, истории перемещений
- ✅ Функционал работает полностью

**Файлы:**
- `frontend/apps/guardian-app/src/components/WardLocationMap.vue`
- `frontend/apps/dispatcher-app/src/components/WardsLocationMap.vue` (если существует)

**Зависимости:**
```json
{
  "leaflet": "^1.9.4",
  "@vue-leaflet/vue-leaflet": "^0.10.0"
}
```

#### Landing Page
- ✅ **Yandex Maps** используется через iframe (встроенный виджет)
- ✅ Не требует API ключа (публичный виджет)
- ✅ Работает корректно

**Файл:** `frontend/apps/landing-app/src/views/HomeView.vue`

---

## ❌ Что отсутствует (требует реализации)

### 1. Backend - Геокодинг

#### Проблема: Нет преобразования координат ↔ адреса

**Текущее состояние:**
- `location-service` хранит только координаты (latitude, longitude)
- Нет методов для:
  - **Обратного геокодинга** (координаты → адрес)
  - **Прямого геокодинга** (адрес → координаты)

**Примеры использования:**
- Отображение адреса в интерфейсе вместо только координат
- Поиск местоположения по адресу при создании геозоны
- Экспорт отчетов с адресами

**Планировалось (из документации):**
- Yandex Geocoder API - приоритет средний
- Google Maps Geocoding API - резервный вариант

**Файлы для проверки:**
- `microservices/location-service/src/application/services/location.service.ts` - нет методов геокодинга
- `microservices/location-service/src/infrastructure/controllers/location.controller.ts` - нет endpoints для геокодинга
- `microservices/location-service/env.example` - нет переменных для API ключей

---

### 2. Переменные окружения

**Отсутствуют в `location-service/env.example`:**
```env
# Геокодинг
YANDEX_GEOCODER_API_KEY=your-yandex-geocoder-api-key
GOOGLE_GEOCODING_API_KEY=your-google-geocoding-api-key
GEOCODING_PROVIDER=yandex  # yandex | google
```

**Примечание:** В `scripts/create-env-examples.sh` есть упоминания `GOOGLE_MAPS_API_KEY` и `YANDEX_MAPS_API_KEY`, но они не используются.

---

## 🔍 Детальный анализ

### Location Service

**Текущая функциональность:**
- ✅ Хранение координат (latitude, longitude)
- ✅ Хранение истории перемещений
- ✅ Проверка геозон
- ✅ Публикация событий нарушений геозон

**Отсутствующая функциональность:**
- ❌ Геокодинг координат в адреса
- ❌ Геокодинг адресов в координаты
- ❌ Кэширование результатов геокодинга
- ❌ Endpoints для геокодинга

**Пример API, который должен быть:**
```typescript
// Обратный геокодинг
GET /locations/geocode/reverse?lat=59.9343&lon=30.3351
→ { address: "г. Санкт-Петербург, ул. Калинина, д. 10" }

// Прямой геокодинг
GET /locations/geocode?address=Санкт-Петербург, ул. Калинина, 10
→ { latitude: 59.9343, longitude: 30.3351 }
```

---

## 📊 Итоговая таблица

| Компонент | Статус | Детали |
|-----------|--------|--------|
| **Frontend - Leaflet/OSM** | ✅ Реализовано | Отображение карт, маркеров, геозон |
| **Frontend - Yandex Maps (landing)** | ✅ Реализовано | Iframe виджет на лендинге |
| **Backend - Хранение координат** | ✅ Реализовано | Location service работает |
| **Backend - Геокодинг** | ❌ Отсутствует | Нет интеграции с геокодинг API |
| **Backend - API ключи** | ❌ Отсутствуют | Нет переменных окружения |
| **Кэширование геокода** | ❌ Отсутствует | Не реализовано |

---

## 🎯 Рекомендации

### Высокий приоритет

1. **Реализовать обратный геокодинг в location-service:**
   - Интегрировать Yandex Geocoder API
   - Добавить endpoint `GET /locations/geocode/reverse`
   - Кэшировать результаты в Redis (опционально)

2. **Добавить переменные окружения:**
   - `YANDEX_GEOCODER_API_KEY` в `location-service/env.example`
   - `GEOCODING_PROVIDER` для выбора провайдера

### Средний приоритет

3. **Реализовать прямой геокодинг:**
   - Endpoint `GET /locations/geocode` (адрес → координаты)
   - Использовать при создании геозон по адресу

4. **Кэширование результатов:**
   - Сохранять результаты геокодинга в Redis
   - Срок жизни кэша: 30 дней (адреса не меняются часто)

### Низкий приоритет

5. **Поддержка нескольких провайдеров:**
   - Создать абстракцию `GeocodingProvider`
   - Реализовать адаптеры для Yandex и Google
   - Переключение через env переменную

---

## 📝 Пример реализации

### Структура файлов

```
microservices/location-service/src/
  infrastructure/
    services/
      geocoding/
        geocoding.service.ts          # Интерфейс геокодинга
        yandex-geocoder.service.ts    # Реализация Yandex
        google-geocoder.service.ts    # Реализация Google (опционально)
    repositories/
      geocode-cache.repository.ts     # Кэш результатов (опционально)
  application/
    services/
      location.service.ts             # Добавить методы геокодинга
  infrastructure/
    controllers/
      location.controller.ts          # Добавить endpoints
```

### Минимальная реализация

1. **Создать `GeocodingService`:**
```typescript
export interface GeocodingService {
  reverseGeocode(lat: number, lon: number): Promise<string | null>;
  geocode(address: string): Promise<{ lat: number; lon: number } | null>;
}
```

2. **Реализовать `YandexGeocoderService`:**
```typescript
@Injectable()
export class YandexGeocoderService implements GeocodingService {
  private readonly apiKey = process.env.YANDEX_GEOCODER_API_KEY;
  
  async reverseGeocode(lat: number, lon: number): Promise<string | null> {
    // HTTP запрос к Yandex Geocoder API
  }
}
```

3. **Добавить endpoints в `LocationController`:**
```typescript
@Get('geocode/reverse')
async reverseGeocode(@Query('lat') lat: number, @Query('lon') lon: number) {
  return this.locationService.reverseGeocode(lat, lon);
}
```

---

## ✅ Заключение

**Текущий статус:** 
- ✅ Frontend полностью работает с картами (Leaflet + OpenStreetMap)
- ✅ Backend геокодинг реализован - Yandex Geocoder API интегрирован

**Статус реализации:** ✅ **Завершено** - геокодинг полностью реализован

---

## 📝 Реализация завершена

**Дата:** 2024-12-08

### Реализованные компоненты:

1. ✅ **GeocodingService** - интерфейс для геокодинга
2. ✅ **YandexGeocoderService** - реализация с Yandex Geocoder API
3. ✅ **LocationService** - методы `reverseGeocode()` и `geocode()`
4. ✅ **LocationController** - endpoints:
   - `GET /locations/geocode/reverse?lat=...&lon=...` - координаты → адрес
   - `GET /locations/geocode?address=...` - адрес → координаты
5. ✅ **Автоматический геокодинг** - метод `getLatestLocation()` теперь возвращает адрес
6. ✅ **Переменные окружения** - `YANDEX_GEOCODER_API_KEY` добавлен в `env.example`
7. ✅ **Зависимости** - `axios` добавлен в `package.json`

### Использование:

```bash
# Обратный геокодинг (координаты → адрес)
GET /locations/geocode/reverse?lat=59.9343&lon=30.3351

# Прямой геокодинг (адрес → координаты)
GET /locations/geocode?address=Санкт-Петербург, ул. Калинина, 10

# Получение последней геолокации с адресом
GET /locations/wards/:wardId/latest
# Теперь возвращает { ..., address: "г. Санкт-Петербург, ..." }
```

### Требования:

1. Получить API ключ Yandex Geocoder: https://developer.tech.yandex.ru/services/
2. Установить в `.env` файле:
   ```env
   YANDEX_GEOCODER_API_KEY=your-api-key-here
   ```
3. Перезапустить `location-service`

