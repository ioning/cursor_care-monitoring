# Статус работы фронтенда с бэкендом

**Дата проверки:** 19 декабря 2025

---

## ✅ Фронтенд приложения

| Приложение | Порт | Статус | URL |
|------------|------|--------|-----|
| Guardian App | 5173 | ✅ Работает | http://localhost:5173 |
| Dispatcher App | 5174 | ✅ Работает | http://localhost:5174 |
| Admin App | 5175 | ✅ Работает | http://localhost:5175 |
| Landing App | 5176 | ✅ Работает | http://localhost:5176 |

**Все фронтенд приложения запущены и доступны!**

---

## ❌ Бэкенд сервисы

| Сервис | Порт | Статус | Примечание |
|--------|------|--------|------------|
| API Gateway | 3000 | ❌ Не отвечает | Требуется запуск |
| Auth Service | 3001 | ❌ Не отвечает | Требуется запуск |
| User Service | 3002 | ❌ Не отвечает | Требуется запуск |
| Device Service | 3003 | ❌ Не отвечает | Требуется запуск |
| Telemetry Service | 3004 | ❌ Не отвечает | Требуется запуск |
| Alert Service | 3005 | ❌ Не отвечает | Требуется запуск |
| Location Service | 3006 | ❌ Не отвечает | Требуется запуск |
| Billing Service | 3007 | ❌ Не отвечает | Требуется запуск |
| Integration Service | 3008 | ❌ Не отвечает | Требуется запуск |
| Dispatcher Service | 3009 | ❌ Не отвечает | Требуется запуск |
| Analytics Service | 3010 | ❌ Не отвечает | Требуется запуск |
| AI Prediction Service | 3011 | ❌ Не отвечает | Требуется запуск |
| Organization Service | 3012 | ❌ Не отвечает | Требуется запуск |

---

## 🔍 Анализ

### Что работает:
- ✅ Все фронтенд приложения запущены и доступны
- ✅ Инфраструктура (PostgreSQL, Redis, RabbitMQ) работает
- ✅ База данных настроена и миграции применены

### Что не работает:
- ❌ Бэкенд сервисы не запущены или не отвечают
- ❌ API Gateway недоступен
- ❌ Интеграция фронтенд-бэкенд не работает

---

## 🔧 Конфигурация фронтенда

### API Endpoints

Все фронтенд приложения настроены на подключение к API Gateway:

**Guardian App:**
- API URL: `http://localhost:3000/api/v1` (по умолчанию)
- Прокси: `/api` -> `http://localhost:3000`
- Файл: `frontend/apps/guardian-app/src/api/client.ts`

**Dispatcher App:**
- API URL: `http://localhost:3000/api/v1` (по умолчанию)
- Файл: `frontend/apps/dispatcher-app/src/api/client.ts`

**Admin App:**
- API URL: `http://localhost:3000/api/v1` (по умолчанию)
- Файл: `frontend/apps/admin-app/src/api/client.ts`

**Landing App:**
- API URL: `http://localhost:3000/api/v1` (по умолчанию)
- Файл: `frontend/apps/landing-app/src/api/client.ts`

### Переменные окружения

Фронтенд приложения используют переменную `VITE_API_URL` для настройки API endpoint.

Если переменная не установлена, используется значение по умолчанию: `http://localhost:3000/api/v1`

---

## 🚀 Решение проблемы

### Запуск бэкенд сервисов

1. **Остановите текущие процессы** (если запущены):
   ```powershell
   # Найдите процессы Node.js
   Get-Process -Name node | Where-Object { $_.Path -like "*cursor_care-monitoring*" }
   
   # Остановите их (если нужно)
   # Get-Process -Name node | Where-Object { $_.Path -like "*cursor_care-monitoring*" } | Stop-Process
   ```

2. **Запустите все сервисы:**
   ```powershell
   cd C:\projects\cursor_care-monitoring
   npm run dev:all
   ```

3. **Подождите 1-2 минуты** для полной инициализации всех сервисов

4. **Проверьте health endpoints:**
   ```powershell
   # API Gateway
   Invoke-WebRequest -Uri "http://localhost:3000/api/v1/health"
   
   # Микросервисы
   Invoke-WebRequest -Uri "http://localhost:3001/health"
   Invoke-WebRequest -Uri "http://localhost:3002/health"
   ```

---

## ✅ Проверка интеграции

После запуска бэкенда проверьте:

### 1. Прямое подключение к API
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/v1/health"
```

### 2. Подключение через прокси фронтенда
```powershell
Invoke-WebRequest -Uri "http://localhost:5173/api/v1/health"
```

### 3. Тест авторизации
Откройте в браузере:
- http://localhost:5173 (Guardian App)
- Попробуйте залогиниться
- Проверьте, что запросы идут на API Gateway

### 4. Проверка в DevTools браузера
1. Откройте http://localhost:5173
2. Откройте DevTools (F12)
3. Перейдите на вкладку Network
4. Попробуйте выполнить действие (логин, регистрация)
5. Проверьте, что запросы идут на `http://localhost:3000/api/v1/...`

---

## 📋 Ожидаемое поведение

### После успешного запуска:

1. **API Gateway** должен отвечать на:
   - http://localhost:3000/api/v1/health
   - http://localhost:3000/api/docs (Swagger документация)

2. **Фронтенд приложения** должны:
   - Загружаться без ошибок
   - Успешно отправлять запросы к API
   - Получать ответы от бэкенда

3. **Интеграция** должна работать:
   - Фронтенд -> API Gateway -> Микросервисы
   - Авторизация работает
   - Данные загружаются

---

## 🔍 Диагностика проблем

### Фронтенд не может подключиться к API

**Симптомы:**
- Ошибки в консоли браузера: "Network Error", "CORS Error"
- Запросы не проходят

**Решение:**
1. Убедитесь, что API Gateway запущен на порту 3000
2. Проверьте CORS настройки в API Gateway
3. Проверьте, что прокси настроен правильно в vite.config.ts

### API Gateway не запускается

**Симптомы:**
- Порт 3000 не слушается
- Ошибки при запуске

**Решение:**
1. Проверьте .env файл в api-gateway
2. Проверьте логи запуска
3. Убедитесь, что порт 3000 свободен

### CORS ошибки

**Симптомы:**
- "Access-Control-Allow-Origin" ошибки в браузере

**Решение:**
1. Проверьте CORS_ORIGIN в api-gateway/.env
2. Убедитесь, что порты фронтенда указаны в CORS_ORIGIN

---

## 📝 Итог

- ✅ **Фронтенд:** Все приложения работают
- ❌ **Бэкенд:** Требуется запуск сервисов
- ⏳ **Интеграция:** Будет работать после запуска бэкенда

**Следующий шаг:** Запустите `npm run dev:all` и подождите 1-2 минуты для инициализации всех сервисов.

